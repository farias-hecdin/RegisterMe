const action_list = {
  ADD_USER: "ADD_USER",
  NEXT_USER: "NEXT_USER",
  REMOVE_USER: "REMOVE_USER",
  SELECT_USER: "SELECT_USER",
  UPDATE_USER: "UPDATE_USER",
};

const reducerUser = function (PREVstate, action) {
  const [type, payload] = action;
  switch (type) {
    default:
      return NEWstate;
  }
};

export { action_list };
export default reducerUser;
